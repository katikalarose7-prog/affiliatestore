# wrangler.toml
# frontend/wrangler.toml

name = "picksy"
compatibility_date = "2024-11-01"

# NO "main" field — assets-only mode
# Cloudflare serves static files directly, no Worker script involved

[assets]
directory = "./dist"
not_found_handling = "single-page-application"